language: ruby
rvm:
  - 2.1
  - 2.2
  - 2.3
  - 2.4
  - 2.5 # 2.6 is tested below
branches:
  only:
    - master
script:
  - bundle exec rake test
matrix:
  include:
    - rvm: 2.6
      script:
        - bundle exec rake test
        - gem install rubocop && rubocop
notifications:
  email: false
